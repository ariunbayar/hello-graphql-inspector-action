enum Currencies {
	USD
}


type Money {
	cents: Int
	currency: Currencies
}

type Query {
  getCategories(parent_id: Int!): [Categories]
}

type Categories {
  name: String!
  description: String
  thumbnail: String
}
}
