enum Currencies {
	USD
}


enum SomeStatus {
  ON_HOLD
  PROGRESS
  SUCCESS
  FAIL
  ALL
}

type User {
  firstname: String!
  lastname: String
  picture: String
}


type Money {
	cents: Int
	currency: Currencies
}

type Query {
  getCategories(parent_id: Int!): [Categories]
}

type Categories {
  name: String!
  description: String
  thumbnail: String
}
