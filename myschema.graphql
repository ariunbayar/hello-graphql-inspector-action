enum Currencies {
	USD
}


enum SomeStatus {
  ON_HOLD
  PROGRESS
  SUCCESS
  FAIL
}


type Money {
	cents: Int
	currency: Currencies
}

type Query {
  getCategories(parent_id: Int!): [Categories]
}

type Categories {
  name: String!
  description: String
  thumbnail: String
}
